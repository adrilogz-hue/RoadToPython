rutas = ["data", "qa", "backend"]

# El índice empieza en 0, no en 1
print(rutas[0])
print(rutas[1])
print(rutas[2])

# len() devuelve cuántos elementos tiene la lista
print(f"Hay {len(rutas)} rutas disponibles")